1. Upload index.js to your Worker.
2. Create a KV.
3. Bind your KV to your Worker.
4. Create an environment variable named `UPLOAD_KEY` and fill in your upload passkey.
 
`<domain>/upload/<your-upload-key>` to upload  
`<domain>/image` to get image
